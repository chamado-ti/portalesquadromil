// Guarita's tickets — reuses Colaborador page entirely (same hook, same UI).
// RLS policy allows guarita to view/create their own tickets.
export { default } from '@/pages/colaborador/ColaboradorChamadosPage';
